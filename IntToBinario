#include <stdio.h>

void intToBinaryArray(int num, int binaryArray[], int var) {
    for (int i = 0; i < var; i++) {
        binaryArray[i] = (num >> (var - 1 - i)) & 1; // Extraer cada bit
    }
}
int main(){
}
